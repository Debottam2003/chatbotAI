<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chat Gpt clone By Debottam</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: inherit;
        width: 100%;
        flex-direction: column;
        background-color: #020120;
        color: aliceblue;
        font-family: Arial, Helvetica, sans-serif;
      }
      .headfer {
        width: 90%;
      }
      .search {
        display: flex;
        margin: 10px;
        height: 300px;
        justify-content: center;
        align-items: end;
        width: 90%;
      }
      form {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
      }
      input,
      button {
        margin: 1rem;
        border-radius: 5px;
        border: none;
        font-family: Arial, Helvetica, sans-serif;
        background-color: #212121;
        color: rgb(200, 203, 207);
        filter: drop-shadow(0 0 0.75rem rgb(58, 58, 230));
      }
      input {
        padding: 1.2rem;
        width: 70%;
        font-family: cursive;
        outline: none;
        font-weight: bold;
        font-size: larger;
      }
      button {
        padding: 1rem;
        background-color: rgb(8, 8, 203);
        font-family: cursive;
        font-weight: bold;
        font-size: large;
        color: rgb(221, 224, 229);
      }
      .result {
        width: 90%;
        color: rgb(200, 203, 207);
        padding: 10px;
      }
      .background_result {
        background-color: black;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>
        Generate Whatever You Want
        <a href="/" style="text-decoration: none; color: rgb(30, 14, 183)"
          >Go Back</a
        >
      </h1>
    </div>
    <div class="search">
      <form action="/ai" method="POST">
        <input type="text" name="prompt" id="ai_data" required />
        <button type="submit">Search</button>
      </form>
    </div>
    <div class="result">
      <div class="background_result">
        <% if(locals.ai_data){ %> <% ai_data.forEach((data) => { %>
        <h3><%= data %></h3>
        <% }) %> <%} %>
      </div>
    </div>
  </body>
</html>
